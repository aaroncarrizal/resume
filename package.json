{
  "private": true,
  "workspaces": [
    "core",
    "adapters/*",
    "scripts"
  ],
  "scripts": {
    "optimize": "tsx --env-file=.env scripts/optimize.ts",
    "pdf": "tsx scripts/pdf.ts",
    "dev": "npm run dev -w @resume/react-renderer",
    "start": "npm run optimize && npm run pdf"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
